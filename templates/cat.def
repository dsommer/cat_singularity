bootstrap: library
From: ubuntu:18.04

%files
	sequences.fasta sequences.fasta

%post
	apt-get update && apt-get -y install emacs

%environment
	export MAX_SEQS=10

%runscript


%labels
	Author dsommer


	
